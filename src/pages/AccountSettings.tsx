/** ODA1_0200 — Page: chỉ layout, UI nằm ở components/account/AccountSettingsForm. */
import AccountSettingsForm from "@/components/account/AccountSettingsForm";

export default function AccountSettings() {
  return (
    <div className="max-w-2xl mx-auto animate-fade-in p-4 md:p-0">
      <AccountSettingsForm />
    </div>
  );
}
